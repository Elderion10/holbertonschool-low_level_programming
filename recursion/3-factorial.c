#include "main.h"
/**
 * factorial - returns the factorial of a
 * given number
 * @n: stores the number
 * Return: -1
 */
int factorial(int n)
{
	factorial(n);

		if (n < 0)
		{
			return (-1);
		}
		return (0);
}
